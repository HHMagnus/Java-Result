plugins {
    id 'com.gradleup.nmcp.settings' version '1.4.4'
}

rootProject.name = 'results'

nmcpSettings {
    centralPortal {
        username = System.getenv("OSSRH_USERNAME")
        password = System.getenv("OSSRH_PASSWORD")
    }
}